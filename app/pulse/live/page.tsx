import PulseDashboard from '@/components/pulse/PulseDashboard'

/** Conference monitor / projector — AI Pulse dashboard (stable URL). */
export default function PulseLivePage() {
  return <PulseDashboard />
}
